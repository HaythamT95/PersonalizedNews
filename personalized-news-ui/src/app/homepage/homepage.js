'use client'

const HomePage = () => {
    return (
        <div>
            <h1>Welcome to Home page</h1>
            <p>This is the page that users are redirected to when they have userData in localStorage.</p>
        </div>
    )
}

export default HomePage;